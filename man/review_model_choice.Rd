% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_choice.R
\name{review_model_choice}
\alias{review_model_choice}
\title{Review linear vs. polynomial model choice across several standard curves}
\usage{
review_model_choice(
  data,
  n_curves = NULL,
  curve_id_col = "unique_curve_id",
  conc_col = "std_conc",
  value_col = "abs_corrected",
  through_origin = TRUE,
  unit_col = NULL,
  colour_col = NULL,
  random_sample = TRUE,
  seed = NULL,
  overplot = FALSE,
  overplot_smooth_alpha = 0.3,
  overplot_smooth_linewidth = 0.8,
  overplot_smooth_linetype = 1,
  legend_position = "right"
)
}
\arguments{
\item{data}{A tibble containing data for multiple standard curves,
with the columns referenced by \code{curve_id_col}, \code{conc_col}, and
\code{value_col}.}

\item{n_curves}{Number of curves to review. If \code{NULL} (the default),
all curves in \code{data} are used. If greater than the number of
available curves, a message is printed and all available curves
are used instead.}

\item{curve_id_col}{Name of the column identifying which curve a row
belongs to. Defaults to \code{"unique_curve_id"}.}

\item{conc_col}{Name of the column containing concentration. Defaults
to \code{"std_conc"}.}

\item{value_col}{Name of the column containing (blank-corrected)
absorbance. Defaults to \code{"abs_corrected"}.}

\item{through_origin}{Whether to force both models through the origin.
Defaults to \code{TRUE}.}

\item{unit_col}{Passed through to \code{\link[=plot_model_comparison]{plot_model_comparison()}} /
\code{plot_std()}. Defaults to \code{NULL} (no unit shown).}

\item{colour_col}{Passed through to \code{\link[=plot_model_comparison]{plot_model_comparison()}} — only
meaningful when \code{overplot = TRUE} (the paginated mode shows one
curve at a time, so colour is moot there). If \code{NULL} (the
default), curves are coloured by \code{curve_id_col}, which can look
like a rainbow with many curves, since curve IDs aren't ordered
meaningfully. Set to something scientifically relevant instead —
e.g. a date or dilution-factor column — to colour by what
actually matters for interpreting the fit.}

\item{random_sample}{Whether to select curves randomly (\code{TRUE}, the
default) or simply take the first \code{n_curves} as they appear in
\code{data}.}

\item{seed}{Optional random seed, for reproducible sampling when
\code{random_sample = TRUE}.}

\item{overplot}{If \code{FALSE} (the default), returns a named list with
one comparison+residual pair per curve. If \code{TRUE}, returns one
combined figure overplotting all selected curves together.}

\item{overplot_smooth_alpha, overplot_smooth_linewidth, overplot_smooth_linetype}{Smooth-curve styling used only when \code{overplot = TRUE} — the
default \code{plot_std()} styling (thin, dashed, faint) is hard to see
with many overlapping curves. Defaults to a more visible
\code{alpha = 0.6}, \code{linewidth = 0.8}, \code{linetype = 1} (solid).}

\item{legend_position}{Passed through to \code{\link[=plot_model_comparison]{plot_model_comparison()}} —
\code{ggplot2} \code{legend.position} value for the comparison plot's
colour legend, relevant when \code{colour_col} is set. Also applied to
the overplot residual plot's own model-type legend (linear vs.
polynomial), so both legends stay visually consistent. Defaults
to \code{"none"}.}
}
\value{
Either a named list (one entry per curve, each with
\code{models}, \code{comparison_plot}, and \code{residual_plot}), or a single
combined \code{patchwork} figure — see \code{overplot}.
}
\description{
Samples (or uses all of) the curves in \code{data}, fits both a linear and
polynomial model to each, and builds comparison + residual plots to
help decide which model is more appropriate. Two display modes: a
paginated list of individual per-curve plots (\code{overplot = FALSE},
the default), or one combined figure overplotting all selected
curves together (\code{overplot = TRUE}), better suited to reviewing a
larger number of curves at once.
}
\examples{
paginated_review <- review_model_choice(std_corrected_TDN, n_curves = 5, seed = 1)
# Look at the polynomial model fit of a single curve
paginated_review$NO3_TDN_25_col1$models$poly
# Look at a single curve
paginated_review[[1]]$comparison_plot /
  (paginated_review[[1]]$residual_plot +
    ggplot2::theme(legend.position = "bottom")) +
  patchwork::plot_annotation(title = names(paginated_review[1]))
# Multiple curve observation
review_model_choice(
  std_corrected_TDN, n_curves = 10, overplot = TRUE,
  legend_position = "bottom", overplot_smooth_alpha = 0.2)
# Multiple curve observation with additional colour aesthetic
review_model_choice(std_corrected_TDN, n_curves = 10, overplot = TRUE, colour_col = "date")
}
\seealso{
\code{\link[=fit_curve_models]{fit_curve_models()}}, \code{\link[=plot_model_comparison]{plot_model_comparison()}}, \code{\link[=plot_residual_comparison]{plot_residual_comparison()}}
}
