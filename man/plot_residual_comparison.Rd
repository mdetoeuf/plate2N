% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_choice.R
\name{plot_residual_comparison}
\alias{plot_residual_comparison}
\title{Plot residuals of linear vs. polynomial models for one curve}
\usage{
plot_residual_comparison(curve_data, models, conc_col = "std_conc")
}
\arguments{
\item{curve_data}{A tibble with data for the curve, with the column
referenced by \code{conc_col}. Must be the same data used to fit
\code{models} (same row order/count) — e.g. via \code{\link[=fit_curve_models]{fit_curve_models()}}.
Note: if \code{curve_data} contains rows with \code{NA} in the fitted
value/concentration columns, \code{lm()} silently drops them, which
can misalign residuals against \code{curve_data}'s concentration
values here. Not currently guarded against — worth keeping curve
data free of \code{NA}s in the relevant columns.}

\item{models}{A named list with \code{linear} and \code{poly} elements, each an
\code{lm} model object — typically the output of \code{\link[=fit_curve_models]{fit_curve_models()}}.}

\item{conc_col}{Name of the column containing concentration. Defaults
to \code{"std_conc"}.}
}
\value{
A ggplot object.
}
\description{
Compares how each model's residuals scatter around zero across the
range of concentrations, for one standard curve. Residuals close to
zero with no systematic pattern indicate a good fit; a curved or
systematic pattern (typically in the linear model's residuals, when a
polynomial fit is actually needed) is a sign that model doesn't
capture the true shape of the curve.
}
\examples{
curve <- std_corrected_TDN |>
  dplyr::filter(unique_curve_id == unique(std_corrected_TDN$unique_curve_id)[9])
models <- fit_curve_models(curve)
plot_residual_comparison(curve, models)
# add a connecting line between points, e.g. useful when reviewing
# several curves at once (see review_model_choice())
plot_residual_comparison(curve, models) + ggplot2::geom_line(alpha = 0.5)
}
\seealso{
\code{\link[=fit_curve_models]{fit_curve_models()}}, \code{\link[=plot_model_comparison]{plot_model_comparison()}}
}
