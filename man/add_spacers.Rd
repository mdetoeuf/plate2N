% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_per_sample_qc.R
\name{add_spacers}
\alias{add_spacers}
\title{Interleave plots with spacer panels for visual gaps in patchwork layouts}
\usage{
add_spacers(plots, gap_size = 0.002)
}
\arguments{
\item{plots}{A list of plot/patchwork objects to interleave with spacers.}

\item{gap_size}{Relative size of each spacer panel, compared to a real
plot's size of \code{1}. Defaults to \code{0.002} (a thin visual gap).}
}
\value{
A list with two elements: \code{plots} (the interleaved list,
spacers included) and \code{sizes} (the matching relative-size vector),
ready to pass to \code{patchwork::wrap_plots(result$plots)} and
\code{patchwork::plot_layout(widths = result$sizes)} (or \verb{heights =} for
a vertical arrangement).
}
\description{
A small utility for adding a visible gap between plots combined with
\code{patchwork}. Simply increasing \code{plot.margin} does not create a gap
between adjacent panels that each have their own background (e.g. a
border drawn via \code{plot.background}), since the background fills the
whole panel's allocated space regardless of margin. This function
instead inserts \code{patchwork::plot_spacer()} panels between the real
plots, sized via \code{patchwork::plot_layout(widths =)}/\verb{heights =}.
}
\examples{
# spaced <- add_spacers(list(plot1, plot2, plot3))
# patchwork::wrap_plots(spaced$plots, nrow = 1) +
#   patchwork::plot_layout(widths = spaced$sizes)
}
