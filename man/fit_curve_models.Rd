% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_choice.R
\name{fit_curve_models}
\alias{fit_curve_models}
\title{Fit both linear and polynomial models for one standard curve}
\usage{
fit_curve_models(
  curve_data,
  conc_col = "std_conc",
  value_col = "abs_corrected",
  through_origin = TRUE
)
}
\arguments{
\item{curve_data}{A tibble containing data for a single standard
curve (e.g. one \code{unique_curve_id}), with the columns referenced
by \code{conc_col} and \code{value_col}.}

\item{conc_col}{Name of the column containing concentration.
Defaults to \code{"std_conc"}.}

\item{value_col}{Name of the column containing (blank-corrected)
absorbance. Defaults to \code{"abs_corrected"}.}

\item{through_origin}{Whether to force both models through the origin
(\verb{0 +} in the model formula). Defaults to \code{TRUE}, appropriate
when blank-corrected absorbance should read zero at zero
concentration. Set to \code{FALSE} if that assumption doesn't hold for
your data (e.g. if the standard curve and samples share the same
blank, matching \code{plot_std()}'s own \code{through_origin} argument).}
}
\value{
A named list with two elements, \code{linear} and \code{poly}, each an
\code{lm} model object.
}
\description{
Fits two models relating absorbance to concentration for a single
standard curve, both forced through the origin (blank-corrected
absorbance should read zero at zero concentration): a linear model
(\code{value ~ 0 + conc}) and a polynomial model
(\code{value ~ 0 + conc + I(conc^2)}). Used to help decide which model fits
better for a given dataset — see \code{\link[=plot_model_comparison]{plot_model_comparison()}} and
\code{\link[=plot_residual_comparison]{plot_residual_comparison()}} to visualize the two fits, or
\code{\link[=review_model_choice]{review_model_choice()}} to do this across several curves at once.
}
\examples{
curve <- std_corrected_TDN |>
  dplyr::filter(unique_curve_id == unique(std_corrected_TDN$unique_curve_id)[9])
models <- fit_curve_models(curve)
summary(models$linear)
summary(models$poly)
}
\seealso{
\code{\link[=plot_model_comparison]{plot_model_comparison()}}, \code{\link[=plot_residual_comparison]{plot_residual_comparison()}}, \code{\link[=review_model_choice]{review_model_choice()}}
}
