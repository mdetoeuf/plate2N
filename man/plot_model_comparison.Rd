% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_choice.R
\name{plot_model_comparison}
\alias{plot_model_comparison}
\title{Plot linear and polynomial model fits side by side for one or more curves}
\usage{
plot_model_comparison(
  curve_data,
  conc_col = "std_conc",
  value_col = "abs_corrected",
  curve_id_col = "unique_curve_id",
  colour_col = NULL,
  through_origin = TRUE,
  unit_col = NULL,
  smooth_alpha = 0.3,
  smooth_linewidth = 0.5,
  smooth_linetype = 2,
  legend_position = "right"
)
}
\arguments{
\item{curve_data}{A tibble with data for one curve, or several curves
at once (grouped/coloured by \code{curve_id_col}) if you want an
overplotted comparison across multiple curves.}

\item{conc_col}{Name of the column containing concentration. Defaults
to \code{"std_conc"}.}

\item{value_col}{Name of the column containing (blank-corrected)
absorbance. Defaults to \code{"abs_corrected"}.}

\item{curve_id_col}{Name of the column identifying which curve a row
belongs to, used for grouping/colouring. Defaults to
\code{"unique_curve_id"}.}

\item{colour_col}{Name of the column used for colour/fill. If \code{NULL}
(the default), uses \code{curve_id_col}. Set independently to colour
by something more meaningful when comparing several curves at
once (e.g. date, dilution factor).}

\item{through_origin}{Whether to force both fits through the origin.
Defaults to \code{TRUE}. See \code{\link[=fit_curve_models]{fit_curve_models()}} for the same
argument's meaning.}

\item{unit_col}{Passed through to \code{plot_std()} — optionally shows a
concentration unit on the x-axis. \code{NULL} (the default) shows no
unit. See \code{plot_std()}'s own \code{unit_col} argument for the full
behavior (literal string or column name).}

\item{smooth_alpha, smooth_linewidth, smooth_linetype}{Passed through
to \code{plot_std()} — control the fitted smooth-curve's visibility.
Defaults match \code{plot_std()}'s own defaults.}

\item{legend_position}{Standard \code{ggplot2} \code{legend.position} value for
the colour legend. Defaults to \code{"none"}. Only meaningful when
\code{colour_col} (or the default curve-id grouping) produces a
legend worth showing — e.g. when comparing multiple curves at
once via \code{\link[=review_model_choice]{review_model_choice()}}'s \code{overplot = TRUE} mode.}
}
\value{
A combined \code{patchwork} figure (linear model plot next to
polynomial model plot).
}
\description{
Wraps \code{plot_std()}, called once with \code{model = "linear"} and once with
\code{model = "poly"}, combined side by side. \code{plot_std()} fits its own
model internally purely for drawing — this function doesn't use
\code{\link[=fit_curve_models]{fit_curve_models()}}'s output, since that's a separate (if
equivalent) fit done for a different purpose (inspecting the actual
model object, e.g. for \code{\link[=plot_residual_comparison]{plot_residual_comparison()}}).
}
\examples{
curve <- std_corrected_TDN |>
  dplyr::filter(unique_curve_id == unique(std_corrected_TDN$unique_curve_id)[9])
plot_model_comparison(curve, unit_col = "std_unit")
}
\seealso{
\code{\link[=fit_curve_models]{fit_curve_models()}}, \code{\link[=plot_residual_comparison]{plot_residual_comparison()}}, \code{\link[=plot_std]{plot_std()}}
}
