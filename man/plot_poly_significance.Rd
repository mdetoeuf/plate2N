% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_choice.R
\name{plot_poly_significance}
\alias{plot_poly_significance}
\title{Plot the distribution of polynomial x² coefficient significance across curves}
\usage{
plot_poly_significance(review, signif_alpha = 0.05)
}
\arguments{
\item{review}{A named list as returned by \code{\link[=review_model_choice]{review_model_choice()}} in
its default, paginated form (\code{overplot = FALSE}) — one entry per
curve, each containing a \code{models$poly} element. Does not accept
the single combined figure returned when \code{overplot = TRUE}.}

\item{signif_alpha}{Significance threshold. Defaults to \code{0.05}. Used
for the vertical threshold line, its accompanying annotations,
and the "N of total curves significant" count.}
}
\value{
A ggplot object.
}
\description{
Companion summary view to \code{\link[=review_model_choice]{review_model_choice()}}'s \code{overplot = TRUE}
mode: while the overplotted comparison/residual plots let you assess
curve shape and fit quality at a glance across a whole sample, this
plots the \emph{significance} of the polynomial term itself across that
same sample — a density of each curve's x² coefficient p-value, on a
log scale, with the number of curves crossing the significance
threshold called out directly. Useful for answering "does switching
to a polynomial model genuinely help, across my whole dataset, or
just for the one curve I happened to look at?"
}
\examples{
review <- std_corrected_TDN |> review_model_choice(n_curves = 10, seed = 1)
plot_poly_significance(review)
plot_poly_significance(review, signif_alpha = 0.01)
}
\seealso{
\code{\link[=review_model_choice]{review_model_choice()}}, \code{\link[=lm_diagnostics]{lm_diagnostics()}}, \code{\link[=fit_curve_model]{fit_curve_model()}}
}
