% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_per_sample_qc.R
\name{plot_ridges_values}
\alias{plot_ridges_values}
\title{Ridge-line density plot of values, grouped by sample}
\usage{
plot_ridges_values(
  data,
  value_col = "conc_mgN_L",
  groups_col = "sample_id",
  colour_col = NULL,
  y_col = "sample_id"
)
}
\arguments{
\item{data}{A tibble containing the columns referenced by \code{value_col},
\code{groups_col}, \code{y_col}, and (if used as a column name) \code{colour_col}.}

\item{value_col}{Name of the numeric column whose density is plotted.
Defaults to \code{"conc_mgN_L"}, but this can equally be raw absorbance
data if you'd rather spot outliers before inferring concentration
(see \code{\link[=boxplot_values]{boxplot_values()}} for the same note on linear vs. polynomial
models).}

\item{groups_col}{Name of the column defining groups (one ridge per unique
value). Defaults to \code{"sample_id"} — replace with whatever column
identifies your own sample units.}

\item{colour_col}{If \code{NULL}, all ridges are drawn in a single, uniform
colour. Otherwise, the name of a column in \code{data} used for colour/fill
aesthetics (e.g. to distinguish ridges by run or batch). Defaults to
\code{NULL}.}

\item{y_col}{Name of the column defining the vertical ridge position.
Defaults to \code{"sample_id"}.}
}
\value{
A ggplot object.
}
\description{
Plots a ridgeline density plot of a numeric column, split into one ridge
per group. This is the companion function to \code{\link[=boxplot_values]{boxplot_values()}}: a ridge
plot is often the easiest way to visually spot an outlier, but doesn't
identify which well to remove — pair it with \code{\link[=boxplot_values]{boxplot_values()}}, which
labels individual points by name, to find the well to pass to
\code{\link[=remove_wells]{remove_wells()}}.
}
\examples{
# plot_ridges_values(my_data, value_col = "co2_g_h", y_col = "plate_id")
}
\seealso{
\code{\link[=boxplot_values]{boxplot_values()}}, \code{\link[=remove_wells]{remove_wells()}}
}
