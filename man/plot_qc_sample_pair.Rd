% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_per_sample_qc.R
\name{plot_qc_sample_pair}
\alias{plot_qc_sample_pair}
\title{Boxplot + ridgeline pair for one chunk of samples}
\usage{
plot_qc_sample_pair(
  chunk,
  data,
  y_col,
  value_col,
  value_range,
  colour_col = NULL,
  scale = 0.8,
  title = NULL,
  border_colour = "grey80",
  legend_position = "none"
)
}
\arguments{
\item{chunk}{A vector of values of \code{y_col} to include in this pair (one
"page" worth of samples).}

\item{data}{A tibble containing the columns referenced by \code{y_col},
\code{value_col}, and (if given) \code{colour_col}.}

\item{y_col}{Name of the column identifying samples — shared between
the boxplot and the ridge plot as the row/category axis.}

\item{value_col}{Name of the numeric column to visualize.}

\item{value_range}{A length-2 numeric vector giving the shared
value-axis range to use, so samples can be compared fairly
regardless of which chunk they land in. Typically computed once
(across the whole dataset) by \code{\link[=plot_list_qc_samples]{plot_list_qc_samples()}} and passed
down.}

\item{colour_col}{Optional colour: \code{NULL} for a default colour, a
literal colour name, or the name of a column in \code{data} — passed
through to both \code{\link[=boxplot_values]{boxplot_values()}} and \code{\link[=plot_ridges_values]{plot_ridges_values()}}.}

\item{scale}{Passed to \code{\link[=plot_ridges_values]{plot_ridges_values()}} — how far each ridge
extends beyond its own row. Defaults to \code{0.8}, which keeps ridges
close enough to their row to stay visually aligned with the paired
boxplot.}

\item{title}{Optional title for this pair, shown centered above the
shared axis. Defaults to \code{y_col}'s name if not given.}

\item{border_colour}{Colour of the border box drawn around the pair.
Defaults to \code{"grey80"}.}

\item{legend_position}{Standard \code{ggplot2} \code{legend.position} value (e.g.
\code{"none"}, \code{"right"}, \code{"bottom"}, \code{"left"}, \code{"top"}), applied to both
the boxplot and the ridge, with duplicate legends automatically
collected into one shared legend for the pair. Defaults to
\code{"none"} (no legend shown).}
}
\value{
A single combined plot (a flattened \code{patchwork} object).
}
\description{
Builds one combined QC figure for a chunk of samples: a labelled
boxplot (\code{\link[=boxplot_values]{boxplot_values()}}) next to a ridgeline density plot
(\code{\link[=plot_ridges_values]{plot_ridges_values()}}), sharing one centered axis of sample
identities between them. The ridge makes it easy to spot an outlier at
a glance; the paired boxplot then gives you the exact well_id to
remove via \code{\link[=remove_wells]{remove_wells()}}. The whole pair is enclosed in a border
box. Typically built once per chunk of samples by
\code{\link[=plot_list_qc_samples]{plot_list_qc_samples()}} rather than called directly.
}
\examples{
# plot_qc_sample_pair(
#   chunk = c("s1", "s2"), data = my_data,
#   y_col = "sample_id", value_col = "conc_mgN_L",
#   value_range = c(0, 10))
}
\seealso{
\code{\link[=plot_list_qc_samples]{plot_list_qc_samples()}}, \code{\link[=boxplot_values]{boxplot_values()}}, \code{\link[=plot_ridges_values]{plot_ridges_values()}}
}
