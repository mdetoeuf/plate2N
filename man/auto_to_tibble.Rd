% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_plates.R
\name{auto_to_tibble}
\alias{auto_to_tibble}
\title{Automatically detect and extract 96-well plate layouts from a raw data file}
\usage{
auto_to_tibble(
  filepath,
  format = NULL,
  delim = "\\t",
  skip = 0,
  comment = "",
  col_select = NULL,
  plate_id_in_anchor = TRUE,
  plate_ids = NULL,
  case_sensitive = TRUE,
  trim_whitespace = TRUE,
  output = "tibble"
)
}
\arguments{
\item{filepath}{Path to the file to scan.}

\item{format}{One of \code{"csv"}, \code{"csv2"}, \code{"xlsx"}, or \code{"txt"}. If \code{NULL}
(the default), guessed from the file extension for \code{.xlsx}/\code{.txt}.
A \code{.csv} extension defaults to \code{"csv"} (comma-delimited) with a
warning, since the extension alone can't distinguish it from a
semicolon-delimited ("French locale") file — pass \code{format = "csv"}
explicitly to silence the warning, or \code{format = "csv2"} if that's
actually what you have.}

\item{delim}{Delimiter used when \code{format = "txt"}. Defaults to \code{"\\t"}.}

\item{skip, comment}{Passed through to the underlying reader
(\code{readr::read_csv()}/\code{read_csv2()}/\code{read_delim()}, or
\code{readxl::read_excel()} for \code{skip} only). \code{comment} is not
supported for \code{format = "xlsx"} — \code{readxl} has no comment-
skipping option, and passing a non-empty \code{comment} with
\code{format = "xlsx"} errors rather than being silently ignored.}

\item{col_select}{Optionally restrict which columns of the raw file
are scanned (e.g. if you know plate data only appears in a
certain range). Defaults to \code{NULL} (scan every column). For
\code{format = "xlsx"}, this expects column \emph{positions} (a numeric
vector), not names or \code{tidyselect} helpers — simpler than
\code{readr}'s own \code{col_select}, which does accept full \code{tidyselect}
syntax for the other formats.}

\item{plate_id_in_anchor}{If \code{TRUE} (the default), each plate's ID is
read from its anchor cell (the cell just above \code{"A"} and left of
\code{"1"}). If \code{FALSE}, IDs come from \code{plate_ids} if given, or are
auto-generated otherwise (\code{"plate1"}, \code{"plate2"}, ..., zero-padded
to match however many plates are found in total).}

\item{plate_ids}{Optional character vector of plate IDs, used only
when \code{plate_id_in_anchor = FALSE}, applied in the order plates
are found (column by column, top to bottom). If its length
doesn't match the number of plates actually found, a warning is
issued (possibly indicating an incomplete/misaligned plate
layout) and the vector is recycled/truncated to fit.}

\item{case_sensitive}{Whether \code{"A"}-\code{"H"} must match case exactly.
Defaults to \code{TRUE}.}

\item{trim_whitespace}{Whether to trim leading/trailing whitespace
before comparing cells. Defaults to \code{TRUE}.}

\item{output}{Either \code{"tibble"} (all plates stacked, default) or
\code{"list"} (one named element per plate) — same convention as
\code{\link[=txt_to_tibble]{txt_to_tibble()}}.}
}
\value{
Depends on \code{output}, matching \code{\link[=txt_to_tibble]{txt_to_tibble()}}'s own
convention.
}
\description{
Scans a file column by column, looking for the signature shape of a
96-well plate: eight consecutive rows reading \code{"A"} through \code{"H"},
with the row immediately above holding \code{1} through \code{12} across the
next 12 columns. Once found, that block (the anchor cell plus 8 rows
and 12 columns) is extracted as one plate; scanning then continues
down the same column for further plates stacked below, before moving
to the next column. Useful for files that don't follow one of the
package's other fixed import formats.
}
\details{
\strong{Only complete plates are detected} — a broken or partial \code{A}-\code{H}
or \code{1}-\code{12} run (e.g. a missing row) will not be recognized, and
that data will simply be skipped rather than flagged.
}
\section{Numeric precision}{

Values are read and returned as character strings, matching the
rest of the package's import functions. For \code{.xlsx} files
specifically, this can occasionally surface long floating-point
artifacts (e.g. \code{"0.31830000000000003"} instead of the displayed
\code{"0.3183"}) — a known \code{readxl} quirk when reading numeric cells as
text, not something this function rounds or otherwise adjusts. If
this matters for your data, round explicitly downstream.
}

\examples{
filepath <- system.file("extdata", "tecan_example/tecan1.xlsx", package = "plate2N")
auto_to_tibble(filepath)
}
