% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_per_sample_qc.R
\name{plot_list_qc_samples}
\alias{plot_list_qc_samples}
\title{Build per-sample QC plots (boxplot + ridgeline pairs), paginated}
\usage{
plot_list_qc_samples(
  data,
  y_col,
  value_col = "conc_mgN_L",
  colour_col = NULL,
  n_chunks = NULL,
  max_samples_per_chunk = 25,
  value_range = NULL,
  scale = 0.8,
  gap_size = 0.002,
  legend_position = "none",
  title = NULL
)
}
\arguments{
\item{data}{A tibble containing the columns referenced by \code{y_col},
\code{value_col}, and (if given) \code{colour_col}.}

\item{y_col}{Name of the column identifying samples.}

\item{value_col}{Name of the numeric column to visualize. Defaults to
\code{"conc_mgN_L"}, but can be set to any per-sample numeric value
(e.g. raw or blank-corrected absorbance) for reuse beyond this
default.}

\item{colour_col}{Optional colour: \code{NULL} for a default colour, a
literal colour name, or the name of a column in \code{data} — passed
through to every pair.}

\item{n_chunks}{Number of chunks (pages/panels) to split samples into.
If \code{NULL} (the default), computed from \code{max_samples_per_chunk}
instead.}

\item{max_samples_per_chunk}{Maximum number of samples per chunk, used
to compute \code{n_chunks} when it isn't given directly. Ignored if
\code{n_chunks} is given. Defaults to \code{25}.}

\item{value_range}{A length-2 numeric vector giving the shared
value-axis range across all chunks, so samples are compared fairly
regardless of which chunk they land in. If \code{NULL} (the default),
computed from \code{data[[value_col]]}'s own range — pass this
explicitly instead (e.g. computed from an unfiltered dataset) when
comparing two versions of the same data side by side (such as
before/after outlier removal), so both use the same scale.}

\item{scale}{Passed to \code{\link[=plot_ridges_values]{plot_ridges_values()}} via each pair. Defaults
to \code{0.8}.}

\item{gap_size}{Relative width of the visual gap between chunks,
passed to \code{\link[=add_spacers]{add_spacers()}}. Defaults to \code{0.002}.}

\item{legend_position}{Standard \code{ggplot2} \code{legend.position} value (e.g.
\code{"none"}, \code{"right"}, \code{"bottom"}, \code{"left"}, \code{"top"}), applied to both
the boxplot and the ridge, with duplicate legends automatically
collected into one shared legend for the pair. Defaults to
\code{"none"} (no legend shown).}

\item{title}{Optional title, passed to every pair (identical across
chunks). Defaults to \code{y_col}'s name if not given.}
}
\value{
A single combined \code{patchwork} figure (one row, one pair-panel
per chunk, evenly spaced) — ready to have an overall title added
via \code{+ patchwork::plot_annotation(title = "...")}.
}
\description{
Splits \code{data} into evenly-sized chunks of samples and builds a
\code{\link[=plot_qc_sample_pair]{plot_qc_sample_pair()}} figure for each chunk, combining them side by
side (with a small visual gap between chunks) into one figure. This is
the per-sample analog of \code{\link[=plot_list_qc_microresp]{plot_list_qc_microresp()}}, generalized
beyond MicroResp: use it for any per-sample outlier-QC step (e.g.
abs-to-conc's downstream per-sample averaging).
}
\examples{
# qc_plot <- plot_list_qc_samples(my_data, y_col = "sample_id", colour_col = "zone") +
#   patchwork::plot_annotation(title = "NO3")
}
\seealso{
\code{\link[=plot_qc_sample_pair]{plot_qc_sample_pair()}}, \code{\link[=plot_list_qc_microresp]{plot_list_qc_microresp()}}
}
